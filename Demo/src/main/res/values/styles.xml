<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseWhiteAppTheme">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/app_splash_bg</item>
        <!--<item name="android:navigationBarDividerColor">@null</item>-->
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/app_splash_bg</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:navigationBarColor">@color/transparent</item>
    </style>

    <style name="AppFlowStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/lib_xhdpi</item>
        <item name="r_flow_item_vertical_space">@dimen/lib_hdpi</item>
        <item name="r_flow_item_horizontal_space">@dimen/lib_hdpi</item>
    </style>

    <style name="DslTabItemStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#FFF</item>
    </style>

    <style name="TabSlidingItemStyle" parent="DslTabItemStyle">
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
    </style>

</resources>
